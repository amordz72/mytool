# AI Rules — MyTool

هذا الملف مرجع مختصر لأي AI يعمل لاحقًا على هذا المستودع.
الهدف منه التفريق بوضوح بين ما تم تنفيذه، وما تم اختباره، وما زال مجرد اقتراح.

## قاعدة الحالة

استخدم واحدة فقط من هذه الحالات لكل فكرة أو ميزة:

- `IMPLEMENTED` = منفذة في الكود.
- `TESTED` = منفذة وتم التحقق منها عمليًا.
- `PROPOSED` = اقتراح فقط، غير منفذ.
- `PENDING` = مطلوب أو متفق عليه لكن لم يُنفذ بعد.

لا تعتبر أي اقتراح منفذًا بدون وجوده فعليًا في الكود.

## الغرض الحقيقي من أداة البطاقات

أداة البطاقات ليست مجرد محول أو مقسم TXT.
هي أداة لإدارة وفصل مخزون البطاقات بين أكثر من مكان أو نظام.
قد تُرفع مجموعة من البطاقات إلى موقع Flexy، ثم يحتاج المستخدم لاحقًا إلى حذف مجموعة محددة من ذلك الموقع حتى تصبح تلك البطاقات حرة وقابلة للاستعمال أو البيع في مكان آخر.
لذلك يجب الحفاظ على هوية كل مجموعة وعدم خلط أو تكرار البطاقات بين الأقسام.

## ميزات حالية

- `TESTED` استخراج بطاقة Mobilis من الرقم الموجود قبل الفاصلة.
- `TESTED` التحقق أن رقم بطاقة Mobilis قبل الفاصلة يساوي 15 رقمًا بالضبط.
- `TESTED` ملفات Mobilis TXT تخرج بدون BOM حتى لا يظهر حرف مخفي في أول بطاقة.
- `IMPLEMENTED` تقسيم البطاقات إلى أقسام مستقلة وتحميل كل قسم في ملف مستقل.
- `IMPLEMENTED` أقفال الأقسام: القسم المقفول لا يغيره البرنامج تلقائيًا.
- `IMPLEMENTED` الشرط الثابت: مجموع أعداد الأقسام يجب أن يساوي دائمًا العدد الأصلي للبطاقات قبل السماح بالتحميل.
- `IMPLEMENTED` أسماء Mobilis المختصرة للملفات على الهاتف.
- `IMPLEMENTED` التحقق من تغير صيغة مصدر Mobilis قبل إنشاء المخرجات.

## أنماط عامة قابلة لإعادة الاستخدام في أدوات أخرى

هذه القواعد ليست خاصة بأداة البطاقات فقط، ويمكن اعتمادها في أدوات TXT والمعالجات والمستوردات والمقسمات المشابهة.

### 1. المصدر الأصلي لا يُعدّل

الحالة: `PENDING` في معالج البطاقات الحالي.

- النص المرفوع أو الملصق يُحفظ كـ Raw Source كما أدخله المستخدم.
- أي Trim أو Normalize أو تنظيف أو استخراج يتم على نسخة مشتقة فقط.
- لا يُكتب المخرج المنظف فوق المصدر الأصلي تلقائيًا.
- إذا احتجنا يومًا لتعديل المصدر نفسه، يجب أن يكون ذلك بفعل صريح ومستقل من المستخدم.

### 2. كل تبويب أو وضع عمل له حالة مستقلة

الحالة: `PENDING` في معالج البطاقات الحالي.

- كل تبويب يحتفظ بالنص والنتيجة والإعدادات الخاصة به.
- التنقل بين التبويبات لا ينقل النص ولا النتيجة إلى تبويب آخر.
- التحميل من تبويب ما يعتمد على حالته هو فقط.

### 3. المسح محلي وليس تحديثًا كاملًا

الحالة: `PENDING` في معالج البطاقات الحالي.

- زر المسح يمسح سياق العمل الحالي فقط.
- لا نطلب من المستخدم تحديث الصفحة كاملة لمسح حقل أو نتيجة.
- إذا كان هناك محتوى مهم، يمكن طلب تأكيد قبل المسح.

### 4. الحفظ المؤقت يجب أن يكون مقصودًا

الحالة: `PROPOSED`.

- لا تُحفظ الأكواد أو النصوص الحساسة في Local Storage تلقائيًا.
- إذا احتجنا استرجاع العمل بعد Refresh، نفضّل حفظ جلسة مؤقت مثل Session Storage بعد حسم السلوك.
- يجب التفريق بين «الاحتفاظ أثناء التنقل داخل الصفحة» و«الاحتفاظ بعد تحديث أو إغلاق الصفحة».

### 5. الحالة المرئية للمستخدم

الحالة: `PROPOSED`.

يمكن للأداة إظهار أن تبويبًا ما يحتوي عملًا محفوظًا، وعدد العناصر، وملخص نتيجة التحقق، حتى لا يضيع المستخدم بين أوضاع متعددة.

## اقتراحات غير منفذة

### اسم اختياري لكل قسم

الحالة: `PROPOSED`

إضافة اسم اختياري لكل قسم عند التقسيم، مثل:
- `Flexy`
- `بيع-خارجي`
- `احتياط`

الاسم يُستخدم للتعريف بالمجموعة، والأفضل أن يظهر في اسم الملف فقط، بدون تغيير محتوى أرقام البطاقات نفسها.
الهدف: معرفة أين خُصصت كل مجموعة وتسهيل نقل المخزون بين الأنظمة بدون خلط.

## مطلوب ولم يُنفذ بعد

### منع رفع الصور والملفات غير النصية

الحالة: `PENDING`

المتصفح على بعض هواتف Android قد يسمح باختيار صورة رغم وجود `accept` في حقل الملفات.
المطلوب لاحقًا إضافة تحقق برمجي بعد اختيار الملف:
- السماح فقط بامتدادات النص المعتمدة مثل `.txt` و`.csv` و`.log`.
- رفض الصور والملفات غير المسموحة قبل قراءتها.
- عدم وضع محتوى الملف المرفوض داخل مربع النص.
- إظهار رسالة واضحة للمستخدم.

ملاحظة: بدأ النقاش حول تنفيذ هذا التحقق ثم أوقفه المستخدم مؤقتًا أثناء اختبار ملفات Mobilis، لذلك لا تعتبره منفذًا.

## قواعد تغيير الكود

- لا تغيّر منطق العمل الأساسي أو معنى التقسيم بدون توضيح أثر التغيير أولًا.
- قبل التعديلات المهمة احفظ نسخة من الإصدار السابق في `archive/`.
- لا تحذف النسخ القديمة من الأرشيف.
- لا تحفظ بيانات بطاقات حقيقية أو ملفات مصدر تحتوي أكواد بطاقات داخل هذا المستودع العام.
- عند السؤال: «ما الذي نفذناه وما الذي لم ننفذه؟» راجع هذا الملف مع الكود الحالي، ولا تعتمد على الذاكرة وحدها.
